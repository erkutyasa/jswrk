/*const users = [
    {
        name: "Erkut",
        age: 28,
        gender: 'male'

    },
    {
        name: "Ahmet",
        age: 30,
        gender: 'male'
    },
    
    {
        name: "Mehmet",
        age: 50,
        gender: 'male'
    },
    {
        name: "Azra",
        age: 35,
        gender: 'female'
    },
]

let totalCount = 0

users.forEach((user) =>
{
    if(user.name.toLocaleLowerCase().includes("a")){
        totalCount++
    }
})
console.log("Toplam isminde a harfi geçen sayısı: ",totalCount)

let  totalAge = 0

users.forEach((user) =>
{
    totalAge += user.age
})
console.log("Kullanıcıların toplam yaşı: ", totalAge)

let totalAvarageAge = 0
users.forEach((user) => 
{
  totalAvarageAge += user.age
})

console.log("kullanıcıların yaş ortalaması: ", totalAvarageAge/users.length)

let totalMale=0
users.forEach((user) => 
{
    if(user.gender=='male')
    {
        totalMale++
    }
})
console.log("Toplam Erkek sayısı: ", totalMale)

let totalFemale = 0
users.forEach((user) => 
{
    if(user.gender=='female')
    {
        totalFemale++
    }
})
console.log("Toplam Kadın sayısı: ", totalFemale)*/

